# ⚠️ IMPORTANT SECURITY NOTICE:
# - DO NOT commit .env files with real secrets to version control
# - Create a local .env file with real values (add to .gitignore)
# - .env.example is safe to commit and serves as documentation
# - All developers must create their own .env file locally
#
# Note: In React, environment variables must be prefixed with REACT_APP_
# to be exposed to client-side code

# Backend API Configuration
# Should match your backend server URL
REACT_APP_API_BASE_URL=http://localhost:5000

# Socket.IO Configuration
# Should match your backend server URL for WebSocket connections
REACT_APP_SOCKET_URL=http://localhost:5000
